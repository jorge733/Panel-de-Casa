import {cookie,readCookies,redirectUri,tokenRequest} from './shared.js';
export default async function handler(req,res){try{let q=req.query||{},cookies=readCookies(req);if(!q.code||!q.state||q.state!==cookies.spotify_state)throw Error('No se pudo validar la conexión. Vuelve a intentarlo.');let d=await tokenRequest({grant_type:'authorization_code',code:q.code,redirect_uri:redirectUri()});res.setHeader('Set-Cookie',[cookie('spotify_refresh',d.refresh_token,60*60*24*180),cookie('spotify_state','',0)]);res.redirect(302,'/?spotify=connected')}catch(e){res.redirect(302,'/?spotify=error')}}
